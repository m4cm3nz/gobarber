import React from 'react';
import { Container } from './styles';

const Dashboard: React.FC = () => {
  return (
    // api.post('users', ({
    //   first_name,
    //   last_name,
    //    user_name,
    //     password,
    //      phonenumber,
    //       email,
    //        birth_date,
    //         gender,
    //          subscriber: false })).then(function(response) {console.log(response)}).catch((error) => { console.log(error) });

    <Container>
      <h1>Hello World</h1>
    </Container>
  );
};

export default Dashboard;
